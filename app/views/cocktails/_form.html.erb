<%= simple_form_for cocktail, class: 'form' do |f| %>
  <%= f.input :name, label: 'Nombre del cocktail' %>
  <%= f.button :submit, class: 'btn btn-primary' %>
  <%= f.input :photo %>
   <%= f.input :photo_cache, as: :hidden %>
<% end %>
